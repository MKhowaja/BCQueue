<UserControl x:Class="BCQueue.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Style="{StaticResource Normal}"
             d:DesignHeight="768" d:DesignWidth="1366">
        <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.4*"/>
            <RowDefinition Height="0.6*"/>
        </Grid.RowDefinitions>
        
        <Button x:Name="CreateButton" Style="{StaticResource CreateButton}"  Grid.Column="0" Grid.Row="1" Margin="150 75 75 75" FontSize="20" Command="{Binding ShowCreateClubInterface}"/>
        <Button x:Name="LoadButton" Style="{StaticResource LoadButton}"  Grid.Column="1" Grid.Row="1" Margin="75 75 150 75" FontSize="20" Command="{Binding ShowLoadClubInterface}"/>
    </Grid>
    
</UserControl>
